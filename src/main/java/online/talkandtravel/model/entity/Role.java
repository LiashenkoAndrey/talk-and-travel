package online.talkandtravel.model.entity;

/**
 * Enum representing different roles a user can have in the application.
 *
 * <ul>
 *   <li>{@code ADMIN} - A user with administrative privileges.
 *   <li>{@code USER} - A regular user with standard access.
 * </ul>
 */
public enum Role {
  ADMIN,
  USER
}
