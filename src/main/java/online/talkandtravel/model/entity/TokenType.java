package online.talkandtravel.model.entity;

/**
 * Enum representing the type of tokens used in the application.
 *
 * <p>This enum specifies different types of tokens. Currently, it includes:
 *
 * <ul>
 *   <li>{@code BEARER} - A token used for bearer authentication.
 * </ul>
 */
public enum TokenType {
  BEARER, PASSWORD_RECOVERY
}
